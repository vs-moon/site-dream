export const useConst = {
  key: Symbol('UP_DIC_DROP'),
  enum: {}
}

export const useEmits = [
  'change'
]

export const useProps = {
  type: {
    type: String,
    default: ''
  }
}

export const useRunning = ({ attrs, slots, emits, props, name }) => {
  return {}
}