export const useOptions = {
  key: Symbol('UP_ICON_DROP'),
  confine: {},
  emits: [],
  props: {
    color: {
      type: String,
      default: 'teal'
    }
  }
}

export const useRunning = ({ attrs, slots, emits, props, name }) => {
  return {}
}